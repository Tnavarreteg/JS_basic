function music(artista, cancion){
    this.artista = artista;
    this.cancion = cancion;
}

for(let i=0; i<4; i++)
{
    let art, can;
    document.write(art, can);
    var musicNueva = new music(art, this.can);
}